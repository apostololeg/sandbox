@import '~theme'

.root
  display flex
  flex-grow 1
  flex-direction column
  overflow hidden
  padding-top var(--indent-s)

.slugWrap
  display flex

.slug
  margin-right var(--indent-s)
  flex-grow 1

.footer
  display flex

.gap
  flex-grow 1

.versionButton
  margin-right var(--indent-s)

.langSwitcherEmpty
  position absolute
  right 10px
  top 10px
