@import '~theme'

.root
  z-index 10
  display flex
  align-items flex-start
  margin 0

.title
  display flex
  align-items center
  flex-grow 1
  min-width 0
  padding 0 var(--indent-s)

  &:first-child
    padding-left 0
