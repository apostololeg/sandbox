@import '~theme'

.root
  height 100vh

.content
  flex-grow 1
  overflow-y auto

.contentCentered
  display flex
  align-items center
  justify-content center

* strong
  text-shadow 0 0 1px var(--accent-color)
