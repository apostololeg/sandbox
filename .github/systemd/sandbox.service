[Unit]
Description=Sandbox Server
After=network.target

[Service]
Type=simple
User={{VPS_USER}}
WorkingDirectory={{VPS_DEPLOY_PATH}}
Environment=NODE_ENV=production
ExecStart=/bin/bash -lc 'cd {{VPS_DEPLOY_PATH}} && NODE_ENV=production node_modules/.bin/babel-node server/index.js'
Restart=always
RestartSec=10
NoNewPrivileges=true

[Install]
WantedBy=multi-user.target
